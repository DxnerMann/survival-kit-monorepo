services:
  postgres:
    image: postgres:16
    container_name: survival-kit-local
    env_file:
      ../.env
    ports:
      - "5432:5432"
  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"
      - "8025:8025"   # Web UI to view emails